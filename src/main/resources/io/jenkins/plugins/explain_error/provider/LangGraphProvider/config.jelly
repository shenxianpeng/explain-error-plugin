<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
  <f:entry title="API Key" field="apiKey">
    <f:password clazz="required"/>
  </f:entry>

  <f:entry title="URL" field="url">
    <f:textbox clazz="required" placeholder="https://your-langgraph-deployment.example.com"/>
  </f:entry>

  <f:entry title="Assistant / Graph ID" field="model"
           description="The assistant UUID or registered graph name to invoke (e.g. my-agent).">
    <f:textbox clazz="required" default="${descriptor.defaultModel}"/>
  </f:entry>

  <f:validateButton title="Test Configuration" progress="Testing..."
                    method="testConfiguration" with="apiKey,url,model" />

</j:jelly>
